#include<iostream>
#include<list>
#include<algorithm>
using namespace std;
int main()
{
	int n,m,t;
	long long int a;
	list<long long int> l;
	cin>>t;
	while(t--)
	{
		cin>>n>>m;
		
		while(n>0)
		{
		
			cin>>a;
			l.push_back(a);
			n--;
		}
		l.sort();
		while(m>0)
		{
			cin>>a;
			/*list<long long int>::iterator i = find(l.begin(), l.end(), a);
			//cout<<*i<<endl;
			if(*i==a)
			{
				cout<<"YES"<<endl;
			}
			else
			cout<<"NO"<<endl;
			l.push_back(a);
			m--;	*/
			int l=,r,mid;
				
		}
		
	}
	return 0;
}
